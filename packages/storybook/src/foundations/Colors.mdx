import { ColorItem, ColorPalette, Meta } from "@storybook/addon-docs/blocks";

import "./Colors.css";

<Meta title="Foundations/Color system" />

export const scale = (family) =>
  Object.fromEntries(
    Array.from({ length: 12 }, (_, index) => {
      const step = index + 1;
      return [`${family}.${step}`, `var(--color-${family}-${step})`];
    })
  );

export const interfaceColors = {
  "surface.canvas": "var(--color-surface-canvas)",
  "surface.subtle": "var(--color-surface-subtle)",
  "text.primary": "var(--color-text-primary)",
  "text.muted": "var(--color-text-muted)",
  "border.subtle": "var(--color-border-subtle)",
  "action.primary": "var(--color-action-primary)",
  "action.primaryHover": "var(--color-action-primary-hover)",
  "action.ghostText": "var(--color-action-ghost-text)",
  "action.ghostHover": "var(--color-action-ghost-hover)",
  "focus.ring": "var(--color-focus-ring)",
};

export const feedbackColors = {
  "feedback.info": "var(--color-feedback-info-text)",
  "feedback.success": "var(--color-feedback-success-text)",
  "feedback.warning": "var(--color-feedback-warning-text)",
  "feedback.error": "var(--color-feedback-error-text)",
};

# Color system

The interface uses a compressed triadic harmony on a neutral field. Electric blue carries agency, hot
magenta carries pressure, and electric amber signals resolution. At solid step `9`, the three anchors
share lightness `0.78` and chroma `0.17`; their hues form `140° / 80° / 140°` gaps.

## Harmony

<div className="color-harmony">
  <div className="color-harmony__swatch color-harmony__swatch--brand">
    <strong>Brand</strong>
    <code>212.66°</code>
    <span>Agency · focus · progress</span>
  </div>
  <div className="color-harmony__swatch color-harmony__swatch--pressure">
    <strong>Pressure</strong>
    <code>352.66°</code>
    <span>Urgency · caution · failure</span>
  </div>
  <div className="color-harmony__swatch color-harmony__swatch--signal">
    <strong>Signal</strong>
    <code>72.66°</code>
    <span>Readiness · success · reward</span>
  </div>
</div>

<div
  className="color-harmony__relation"
  aria-label="Hue gaps: 140 degrees from brand to pressure, 80 from pressure to signal, and 140 from signal to brand"
>
  <span>140° · 80° · 140°</span>
</div>

Pressure and signal sit symmetrically around the brand complement. The `40°` compression narrows
their mutual gap while preserving equal separation from brand.

## Chromatic hierarchy

<div className="color-usage" aria-label="Relative chromatic emphasis: 60 brand, 30 pressure, 10 signal">
  <div className="color-usage__brand">
    <strong>60</strong>
    <span>Brand</span>
  </div>
  <div className="color-usage__pressure">
    <strong>30</strong>
    <span>Pressure</span>
  </div>
  <div className="color-usage__signal">
    <strong>10</strong>
    <span>Signal</span>
  </div>
</div>

These proportions guide chromatic moments rather than total page area. Neutral surfaces remain the
field; signal stays rare enough to retain meaning.

<div className="color-gradient">
  <strong>Expressive accent</strong>
  <span>Three localized glows add energy without interpolating through a rainbow of extra hues.</span>
</div>

## Semantic colors

Components consume semantic aliases so palette adjustments stay separate from component intent.

<ColorPalette>
  <ColorItem title="Interface roles" subtitle="Public component contracts" colors={interfaceColors} />
  <ColorItem title="Feedback roles" subtitle="Three hues, explicit intent" colors={feedbackColors} />
</ColorPalette>

<div className="feedback-grid">
  <div className="feedback feedback--info">
    <span aria-hidden="true">i</span>
    <strong>Information</strong>
  </div>
  <div className="feedback feedback--success">
    <span aria-hidden="true">✓</span>
    <strong>Saved</strong>
  </div>
  <div className="feedback feedback--warning">
    <span aria-hidden="true">!</span>
    <strong>Check input</strong>
  </div>
  <div className="feedback feedback--error">
    <span aria-hidden="true">×</span>
    <strong>Upload failed</strong>
  </div>
</div>

Feedback never depends on hue alone. Labels and icons carry meaning; surface, border, and text steps
increase emphasis inside the three-family grammar.

## Primitive scales

Steps move from the darkest surface value at `1` to the lightest text value at `12`. Chroma peaks
near the solid-control range.

<ColorPalette>
  <ColorItem title="color.neutral" subtitle="Surfaces, borders, and text" colors={scale("neutral")} />
  <ColorItem title="color.brand" subtitle="Primary Agora actions and emphasis" colors={scale("brand")} />
  <ColorItem title="color.pressure" subtitle="Narrative intensity and adverse feedback" colors={scale("pressure")} />
  <ColorItem title="color.signal" subtitle="Readiness, successful outcomes, and rewards" colors={scale("signal")} />
</ColorPalette>

## Authoring rule

Components use semantic color tokens and shared metric tokens. Verify every foreground/background
pairing with the accessibility panel; a visual palette step does not guarantee sufficient contrast.
